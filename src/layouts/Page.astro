---
import Layout from './Base.astro';

export interface Props {
    description: string,
    section: string,
    title: string,
}
const { content } = Astro.props as Props;

---

<Layout title={content.title} description={content.description} section={content.description}>
    <h1>{content.title}</h1>

    <slot />
</Layout>